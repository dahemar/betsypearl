---
import Base from '../layouts/Base.astro';
import { getSiteData } from '../lib/cms/getSite';

const { aboutText } = await getSiteData(Astro.locals);
---

<Base title="about">
  <section class="simple-page">
    <div class="about-copy">
      <p>{aboutText}</p>
    </div>
  </section>
</Base>
